# Data downloaders

```
+-- MapScrape-master             [The maps downloader from J.Friedman we used to download GMaps-custom dataset]
+-- gmaps_osm                    [Our maps downlaoder to download google satelite images and osm labels]
+-- locations_custom.json        [Our json file containing the locations used in the GMaps-custom dataset]
```
